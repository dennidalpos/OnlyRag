<Window x:Class="OnlyRag.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="OnlyRag"
        Height="720"
        Width="1120"
        MinHeight="560"
        MinWidth="900"
        Icon="Assets/OnlyRag.ico"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A">
    <Grid Background="#0F172A">
        <wv2:WebView2 x:Name="MainWebView" />

        <Border x:Name="LoadingPanel"
                Visibility="Visible"
                Background="#0F172A"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⚡ OnlyRag"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="#38BDF8"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16" />
                <ProgressBar IsIndeterminate="True"
                             Width="240"
                             Height="6"
                             Foreground="#38BDF8"
                             Background="#1E293B"
                             BorderThickness="0"
                             Margin="0,0,0,16" />
                <TextBlock x:Name="LoadingStatusText"
                           Text="Avvio del server backend in corso..."
                           FontSize="14"
                           Foreground="#94A3B8"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <Border x:Name="StartupErrorPanel"
                Visibility="Collapsed"
                MaxWidth="560"
                Padding="28"
                BorderBrush="#334155"
                BorderThickness="1"
                Background="#1E293B"
                CornerRadius="8"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock x:Name="StartupErrorTitle"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#F8FAFC"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="StartupErrorMessage"
                           Margin="0,12,0,0"
                           FontSize="14"
                           LineHeight="21"
                           Foreground="#94A3B8"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
