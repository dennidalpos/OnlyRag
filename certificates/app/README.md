# App Certificates

This folder is reserved for public certificate material used for enterprise trust distribution.

Do not store private keys, PFX files, passwords, signing tokens, or other secrets in this repository.
The signing scripts expect private signing material to stay outside the repository or in the Windows
certificate store.
