# Script Inventory

Run repository scripts from the repository root in PowerShell 7 unless a script documents another working directory. Generated outputs under `bin`, `obj`, `dist`, `node_modules`, and `artifacts` are not source script locations.

## Repository Scripts

| Nome | Percorso | Stato | Funzione | Quando usarlo | Invocato da | Input | Output | Side effect | Prerequisiti | Compatibilità | Note |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `Bootstrap-Prerequisites.ps1` | `scripts\Bootstrap-Prerequisites.ps1` | Attivo | Verifica prerequisiti Windows, prepara `%LOCALAPPDATA%\OnlyRag`, ripristina .NET, installa dipendenze web e prepara OCR opzionale. | Setup sviluppo o fresh checkout. | `README.md`, `docs\OPERATIONS.md`, documentazione OCR. | Switch `-SkipNode`, `-SkipOcr`, `-SkipOllamaCheck`, `-NonInteractive`, `-LibreOfficePath`. | Restore .NET, `npm ci`, ambiente OCR opzionale. | Crea directory locali utente e venv OCR opzionale. | Windows, PowerShell 7, .NET 10 SDK/runtimes, WebView2, Node/npm; opzionali Python, Ollama, LibreOffice. | Windows/PowerShell primario. | Non builda, non pacchettizza, non firma e non rilascia. |
| `Build-Web.ps1` | `scripts\Build-Web.ps1` | Attivo | Esegue install npm da lockfile e build React/Vite. | Quando servono asset statici aggiornati in `src\OnlyRag.Web\dist`. | `README.md`, `docs\OPERATIONS.md`, `Build-Installer.ps1`, `Invoke-Gate.ps1`. | Switch `-SkipInstallWhenUpToDate`. | `src\OnlyRag.Web\dist`. | Può aggiornare `node_modules`. | Node.js/npm compatibili con `src\OnlyRag.Web\package.json`. | Windows/PowerShell primario; npm cross-platform nel frontend. | Usa `scripts\support\BuildSupport.ps1`. |
| `Build-App.ps1` | `scripts\Build-App.ps1` | Attivo | Esegue `dotnet restore` e `dotnet build` su `OnlyRag.sln`. | Build .NET locale Debug o Release. | `README.md`, `docs\OPERATIONS.md`, `Invoke-Gate.ps1`. | `-Configuration`, `-NoRestore`. | Output `bin`/`obj` dei progetti .NET. | Ripristina pacchetti NuGet salvo `-NoRestore`. | .NET 10 SDK. | Windows primario; .NET CLI cross-platform dove il target lo consente. | Non esegue test. |
| `Invoke-Gate.ps1` | `scripts\Invoke-Gate.ps1` | Attivo, gate canonico | Gate operativo unico: preflight, restore web/.NET, audit npm/NuGet, typecheck, lint, format check, test frontend, test .NET, self-test installer, build web e build .NET; installer opzionale. | Prima di PR, handoff, CI o release candidate locale. | `.github\workflows\ci.yml`, `README.md`, `docs\OPERATIONS.md`. | `-Configuration`, `-IncludeInstaller`, `-InnoSetupCompiler`. | Build/test output e report dei tool richiamati. | Aggiorna `node_modules`, `dist`, `bin`, `obj`; con `-IncludeInstaller` aggiorna `artifacts`. | Windows, PowerShell 7, .NET 10 SDK, Node/npm, Microsoft Edge per smoke Playwright; Inno Setup solo con `-IncludeInstaller`. | Windows/PowerShell primario. | Fallisce al primo errore con exit code non-zero. |
| `Clean.ps1` | `scripts\Clean.ps1` | Attivo | Rimuove output generati e cache locali e verifica che non restino path ignorati. | Prima di una build pulita, prima di handoff, o per riportare il repository senza output generati. | Uso manuale. | `-PreserveArtifacts`, `-PreserveDependencies`, `-WhatIf`; `-IncludeArtifacts` e `-IncludeDependencies` restano compatibili. | Report finale di pulizia repository. | Rimuove `bin`, `obj`, `dist`, `.vite`, `TestResults`, `coverage`, `playwright-report`, `test-results`, `output`, `__pycache__`, `*.tsbuildinfo`, `*.pyc`, `artifacts` e `node_modules`; segnala senza rimuovere modifiche sorgente. | PowerShell 7, Git per il controllo finale. | Windows/PowerShell primario. | Non tocca `.git`, `certificates`, modifiche tracciate o nuovi file sorgente. |
| `Build-Installer.ps1` | `scripts\Build-Installer.ps1` | Attivo | Build web, publish WPF self-contained `win-x64`, validazione payload e compilazione installer Inno Setup. | Creazione installer non firmato o firmato tramite thumbprint. | `README.md`, `docs\OPERATIONS.md`, `packaging\README.md`, `Sign-Release.ps1`, opzionalmente `Invoke-Gate.ps1 -IncludeInstaller`. | Configurazione, runtime, versione, output, Inno Setup, thumbprint firma, timestamp server, signtool. | `artifacts\publish\...`, `artifacts\installer\OnlyRag-Setup-*.exe`. | Ricrea publish e installer output. | .NET 10 SDK, Node/npm, Inno Setup 6; opzionali `signtool.exe` e certificato installato. | Windows/PowerShell primario. | Usa `scripts\support\BuildSupport.ps1`. |
| `Sign-Release.ps1` | `scripts\Sign-Release.ps1` | Attivo | Importa un PFX esterno o usa un thumbprint installato, builda, firma e verifica l'installer. | Release candidate firmate. | `docs\SIGNING.md`, `PROJECT_STATUS.json`. | `-CertificatePath` esterno, `-CertificateThumbprint`, `-CertificatePassword`, versione, output, tool path. | Installer firmato e verifica release non invasiva salvo `-SkipReleaseVerification`. | Può importare temporaneamente il certificato in `Cert:\CurrentUser\My` e poi rimuoverlo. | Certificato code-signing esterno o installato, Windows SDK `signtool.exe`, Inno Setup 6, prerequisiti build. | Windows/PowerShell primario. | Rifiuta PFX privati dentro il repository. |
| `Export-EnterpriseSigningCertificate.ps1` | `scripts\Export-EnterpriseSigningCertificate.ps1` | Attivo | Esporta il certificato pubblico `.cer` dal signer dell'installer, da PFX esterno o da thumbprint. | Preparazione GPO/MDM per certificato self-signed in ambienti gestiti. | `docs\SIGNING.md`, `PROJECT_STATUS.json`. | `-InstallerPath`, `-CertificatePath` esterno, `-CertificateThumbprint`, `-OutputPath`. | `.cer` pubblico, default `certificates\app\OnlyRag-Enterprise-CodeSigning.cer`. | Crea la directory di output se manca. | Installer firmato o certificato disponibile. | Windows/PowerShell primario. | Non esporta chiavi private; rifiuta PFX privati dentro il repository. |
| `Test-EnterpriseSigningTrust.ps1` | `scripts\Test-EnterpriseSigningTrust.ps1` | Attivo | Verifica trust enterprise in `LocalMachine\Root` e `LocalMachine\TrustedPublisher`; opzionalmente controlla signer installer. | Dopo applicazione GPO su macchina target/domain-joined. | `docs\SIGNING.md`, `PROJECT_STATUS.json`. | Thumbprint certificato, opzionale installer. | Esito test su console. | Nessuno. | Certificato distribuito via policy; opzionale installer firmato. | Windows/PowerShell primario. | Fallisce finche la GPO non ha popolato entrambi gli store richiesti. |
| `Test-InstallerPrerequisites.ps1` | `scripts\Test-InstallerPrerequisites.ps1` | Attivo | Verifica prerequisiti bloccanti Windows 10 1809+ e WebView2; include self-test simulato. | Validazione setup/preflight senza installare. | `Invoke-Gate.ps1`, `README.md`, `docs\OPERATIONS.md`, `packaging\README.md`. | `-SelfTest`, simulazioni WebView2/Windows. | Esito test su console. | Nessuno. | PowerShell 7; Windows per rilevazione reale. | Windows/PowerShell primario. | Il gate usa `-SelfTest`. |
| `Test-InstallerRelease.ps1` | `scripts\Test-InstallerRelease.ps1` | Attivo | Produce evidenza JSON per installer; opzionalmente esegue lifecycle install/upgrade/uninstall/rollback. | Verifica release installer, prima non invasiva e poi su macchina pulita con `-RunInstallLifecycle`. | `README.md`, `docs\OPERATIONS.md`, `docs\SIGNING.md`, `packaging\README.md`, `Sign-Release.ps1`. | Installer primario, installer upgrade/rollback, output, `-RunInstallLifecycle`, `-RequireSigned`. | `artifacts\release-verification\OnlyRag-release-verification-*.json`. | Con lifecycle installa/disinstalla nel profilo utente corrente. | Installer `.exe`; opzionali firma valida e macchina Windows pulita. | Windows/PowerShell primario. | Usa `scripts\support\BuildSupport.ps1`. |
| `Generate-BrandAssets.ps1` | `scripts\Generate-BrandAssets.ps1` | Attivo | Rigenera asset brand e copie integrate per web, GitHub README e setup. | Quando cambia `src\OnlyRag.App\Assets\OnlyRag.svg` o servono asset social/setup aggiornati. | `assets\brand\README.md`, `docs\BRAND_ASSETS.md`. | Nessuno. | Asset sotto `assets\brand`, `.github\assets`, `src\OnlyRag.Web\public`. | Sovrascrive asset generati. | Windows, PowerShell 7, assembly WPF `PresentationCore`/`WindowsBase`. | Solo Windows. | File grande per natura generativa; non e nel gate. |
| `BuildSupport.ps1` | `scripts\support\BuildSupport.ps1` | Helper interno | Funzioni condivise per build web, packaging, signing, validazione payload e rimozioni robuste. | Non usare direttamente. | `Build-Web.ps1`, `Build-Installer.ps1`, `Clean.ps1`, `Sign-Release.ps1`, `Test-EnterpriseSigningTrust.ps1`, `Test-InstallerRelease.ps1`. | Parametri delle funzioni importate. | Dipende dal chiamante. | Dipende dal chiamante. | PowerShell 7. | Windows/PowerShell primario. | Helper interno. |
| `BootstrapPrerequisites.Core.ps1` | `scripts\support\BootstrapPrerequisites.Core.ps1` | Helper interno | Funzioni condivise per preflight, prerequisiti locali, WebView2, LibreOffice e directory runtime. | Non usare direttamente. | `Bootstrap-Prerequisites.ps1`. | Variabili e parametri dello script chiamante. | Dipende dal chiamante. | Dipende dal chiamante. | PowerShell 7. | Windows/PowerShell primario. | Dot-sourced dal bootstrap per mantenere l'entrypoint sotto soglia. |
| `BootstrapPrerequisites.Ocr.ps1` | `scripts\support\BootstrapPrerequisites.Ocr.ps1` | Helper interno | Funzioni OCR del bootstrap: manifest, spazio disco, pinning pacchetti e preparazione venv. | Non usare direttamente. | `Bootstrap-Prerequisites.ps1`. | Variabili e parametri dello script chiamante. | Ambiente OCR opzionale. | Può creare o aggiornare il venv OCR utente. | PowerShell 7, Python opzionale. | Windows/PowerShell primario. | Dot-sourced dal bootstrap per separare la responsabilità OCR. |
| `BrandAssetDrawing.ps1` | `scripts\support\BrandAssetDrawing.ps1` | Helper interno | Primitive di disegno WPF usate dal generatore asset brand. | Non usare direttamente. | `Generate-BrandAssets.ps1`. | Variabili colore e assembly WPF caricati dallo script chiamante. | Bitmap/logo renderizzati dal chiamante. | Dipende dal chiamante. | PowerShell 7, WPF `PresentationCore`/`WindowsBase`. | Solo Windows. | Mantiene il generatore pubblico focalizzato sulla sequenza di output. |
| `paddle_ocr_bridge.py` | `scripts\ocr\paddle_ocr_bridge.py` | Helper runtime | Bridge runtime OCR PaddleOCR invocato dall'app e dal bootstrap. | Runtime OCR o check ambiente OCR. | `Bootstrap-Prerequisites.ps1`, codice applicativo, copy MSBuild da `OnlyRag.Infrastructure.csproj`. | `--mode check|prepare|ocr` e opzioni OCR incluse `--device cpu|gpu`. | JSON su stdout; immagini preprocessate in `prepare`. | Può creare immagini preprocessate nella directory richiesta. | Python 3.10 through 3.13 e pacchetti OCR pinning. | Runtime Python su Windows primario. | In modalità GPU verifica CUDA e non ricade silenziosamente a CPU. |
| `requirements.txt` | `scripts\ocr\requirements.txt` | Manifest runtime | Entry point CPU compatibile per pinning OCR. | Usato dal bootstrap o dalla UI di provisioning OCR CPU. | `Bootstrap-Prerequisites.ps1`, app Settings. | Nessuno. | Dipendenze pip installate nel venv utente. | Installazione pacchetti nel venv OCR. | Python/pip. | Windows primario. | Include `requirements-cpu.txt`. |
| `requirements-common.txt` | `scripts\ocr\requirements-common.txt` | Manifest runtime | Pacchetti OCR condivisi tra CPU e NVIDIA. | Usato dai manifest runtime OCR. | `requirements-cpu.txt`, `requirements-nvidia-cu118.txt`, `requirements-nvidia-cu126.txt`. | Nessuno. | Dipendenze OCR comuni. | Installazione pacchetti nel venv OCR. | Python/pip. | Windows primario. | Non installa PaddlePaddle. |
| `requirements-cpu.txt` | `scripts\ocr\requirements-cpu.txt` | Manifest runtime | Runtime PaddlePaddle CPU pinning. | Runtime OCR CPU o fallback auto. | `requirements.txt`, app Settings. | Nessuno. | `paddlepaddle` CPU installato. | Installazione pacchetti nel venv OCR. | Python/pip. | Windows primario. | Default compatibile. |
| `requirements-nvidia-cu118.txt` | `scripts\ocr\requirements-nvidia-cu118.txt` | Manifest runtime | Runtime NVIDIA CUDA 11.8 pinning. | GPU OCR con driver compatibile CUDA 11.8. | App Settings provisioning OCR auto/NVIDIA. | Nessuno. | `paddlepaddle-gpu` installato da indice PaddlePaddle CUDA 11.8. | Installazione pacchetti nel venv OCR. | Python/pip, NVIDIA driver compatibile. | Windows primario. | Usato solo quando il resolver sceglie `cuda118`. |
| `requirements-nvidia-cu126.txt` | `scripts\ocr\requirements-nvidia-cu126.txt` | Manifest runtime | Runtime NVIDIA CUDA 12.6 pinning. | GPU OCR con driver compatibile CUDA 12.6. | App Settings provisioning OCR auto/NVIDIA. | Nessuno. | `paddlepaddle-gpu` installato da indice PaddlePaddle CUDA 12.6. | Installazione pacchetti nel venv OCR. | Python/pip, NVIDIA driver compatibile. | Windows primario. | Preferito quando il resolver sceglie `cuda126`. |
| `requirements-nvidia-cu129.txt` | `scripts\ocr\requirements-nvidia-cu129.txt` | Manifest runtime | Runtime NVIDIA CUDA 12.9 pinning. | GPU OCR con driver compatibile CUDA 12.9. | App Settings provisioning OCR auto/NVIDIA. | Nessuno. | `paddlepaddle-gpu` installato da indice PaddlePaddle CUDA 12.9. | Installazione pacchetti nel venv OCR. | Python/pip, NVIDIA driver compatibile. | Windows primario. | Preferito quando il resolver sceglie `cuda129`. |
| `runtime-manifest.json` | `scripts\ocr\runtime-manifest.json` | Manifest runtime | Metadati OCR: versioni attese, profili runtime, disco libero, cache modelli. | Verifica OCR runtime. | `Bootstrap-Prerequisites.ps1`, app Settings. | Nessuno. | Esito verifica pacchetti. | Nessuno. | Python OCR preparato. | Windows primario. | Elenca CPU e target NVIDIA supportati. |

## Frontend Package Scripts

Working directory: `src\OnlyRag.Web`. npm e il package manager supportato perche `package-lock.json` e presente.

| Nome | Percorso | Stato | Funzione | Quando usarlo | Invocato da | Input | Output | Side effect | Prerequisiti | Compatibilità | Note |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `dev` | `src\OnlyRag.Web\package.json` | Attivo | Avvia Vite su `127.0.0.1`. | Sviluppo UI con desktop app in Debug. | Uso manuale/documentazione operativa. | Variabili Vite standard. | Dev server. | Mantiene processo in foreground. | Node/npm e dipendenze installate. | Windows e ambienti Node supportati. | Non usato da CI; il WPF host accetta solo dev-server URL loopback prima di iniettare il bridge backend. |
| `build` | `src\OnlyRag.Web\package.json` | Attivo | Esegue `tsc -b` e `vite build`. | Build asset web statici. | `Build-Web.ps1`, `Build-Installer.ps1`. | Config TypeScript/Vite. | `src\OnlyRag.Web\dist`. | Sovrascrive asset build. | Node/npm e dipendenze installate. | Windows e ambienti Node supportati. | Output consumato dalla WPF app. |
| `typecheck` | `src\OnlyRag.Web\package.json` | Attivo | Esegue TypeScript senza emit. | Verifica frontend. | `Invoke-Gate.ps1`, documentazione operativa. | Sorgenti frontend. | Esito su console. | Nessuno. | Node/npm e dipendenze installate. | Windows e ambienti Node supportati. | Separato da lint e format check. |
| `lint` | `src\OnlyRag.Web\package.json` | Attivo | Esegue ESLint. | Verifica statica frontend. | `Invoke-Gate.ps1`, documentazione operativa. | Sorgenti frontend. | Esito su console. | Nessuno. | Node/npm e dipendenze installate. | Windows e ambienti Node supportati. | Non riscrive file. |
| `test` | `src\OnlyRag.Web\package.json` | Attivo | Esegue test unit/component Vitest e smoke e2e Playwright. | Verifica regressioni UI e flussi frontend principali. | `Invoke-Gate.ps1`, documentazione operativa. | Sorgenti frontend e mock API locali ai test. | Esito su console, trace Playwright solo in caso di failure. | Avvia Vite temporaneamente durante lo smoke e2e. | Node/npm, dipendenze installate, Microsoft Edge. | Windows primario; Vitest cross-platform, e2e configurato su Edge. | Comando aggregato di `test:unit` e `test:e2e`. |
| `test:unit` | `src\OnlyRag.Web\package.json` | Attivo | Esegue test unitari e component con Vitest/jsdom. | Durante sviluppo frontend rapido. | `test`. | Sorgenti `src/**/*.test.ts(x)`. | Esito su console. | Nessuno. | Node/npm e dipendenze installate. | Windows e ambienti Node supportati. | Copre API client, lifecycle, helper Settings, Chat, Documents e Settings. |
| `test:e2e` | `src\OnlyRag.Web\package.json` | Attivo | Esegue smoke Playwright con backend mockato. | Prima di handoff frontend o gate completo. | `test`. | App Vite e route API mockate. | Esito su console, `test-results`/`playwright-report` in caso di failure. | Avvia Vite temporaneamente. | Node/npm, dipendenze installate, Microsoft Edge. | Windows primario. | Copre import, job state, backend offline e preview modal. |
| `format:check` | `src\OnlyRag.Web\package.json` | Attivo | Esegue Prettier in check mode e `scripts\check-format.mjs`. | Verifica non invasiva della formattazione frontend. | `Invoke-Gate.ps1`, documentazione operativa. | Manifest/config/script frontend e testi sorgente supportati. | Esito su console. | Nessuno. | Node/npm e dipendenze installate. | Windows e ambienti Node supportati. | Non applica formattazione automatica. |
| `preview` | `src\OnlyRag.Web\package.json` | Attivo manuale | Avvia preview Vite su `127.0.0.1`. | Preview manuale degli asset buildati. | Uso manuale. | Build web gia prodotta. | Preview server. | Mantiene processo in foreground. | Node/npm e dipendenze installate. | Windows e ambienti Node supportati. | Non usato da CI o packaging. |
| `check-format.mjs` | `src\OnlyRag.Web\scripts\check-format.mjs` | Helper interno | Controlla newline finale e trailing whitespace su file frontend supportati. | Solo tramite `npm run format:check`. | `package.json`. | Sorgenti frontend. | Esito su console. | Nessuno. | Node.js. | Windows e ambienti Node supportati. | Non e un comando repository primario. |

## CI e Task Runner

| Nome | Percorso | Stato | Funzione | Invocatori | Note |
|---|---|---|---|---|---|
| `ci.yml` | `.github\workflows\ci.yml` | Attivo | Installa .NET 10, Node LTS e lancia `.\scripts\Invoke-Gate.ps1 -Configuration Release`. | GitHub Actions push su `main` e pull request. | Il gate CI e allineato al gate locale. |

## Migrazioni e Script Non Sorgente

- `scripts\Test-All.ps1` non esiste piu: usare `scripts\Invoke-Gate.ps1`.
- `scripts\agents\Gate-Build.ps1` non esiste piu: usare `scripts\Invoke-Gate.ps1`; per packaging aggiungere `-IncludeInstaller`.
- `scripts\internal\BuildSupport.ps1` non esiste piu: l'helper e `scripts\support\BuildSupport.ps1`.
- `assets\brand\Generate-BrandAssets.ps1` non esiste piu: usare `scripts\Generate-BrandAssets.ps1`.
- `scripts\script.md` non esiste piu: l'inventario canonico e questo file.
- `certificates\app\code_signing.ps1` non e supportato ed e stato rimosso perche generava PFX e password nel repository; usare `scripts\Sign-Release.ps1` con materiale di signing esterno o certificati installati nello store Windows.
